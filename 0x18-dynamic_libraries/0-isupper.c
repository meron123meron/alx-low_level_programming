#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @c:is character used
 * Return:0
 */
int _isupper(int c)
{

for (c = 'A'; c <= 'Z'; c++)
{
return (1);
}
for (c = 'a'; c <= 'z'; c++)
{
return (48);
}
return (0);
}
