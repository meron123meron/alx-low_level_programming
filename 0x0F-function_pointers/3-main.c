#include <stdio.h>
#include "3-calc.h"
/**
 * main - main function
 *
 */
int main(void)
{
int (*get_op_func(char *s))(int, int)
{

s = {op_add, op_sub, op_mul, op_div, op_mod};
(*get_op_func(s[i]))(a, b);
return (0);
}
